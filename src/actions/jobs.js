export const FETCH_NEWSFEED = (data) => ({
  type: 'FETCH_NEWSFEED',
  data,
});

export const FETCH_MORE_NEWSFEED = (data) => ({
  type: 'FETCH_MORE_NEWSFEED',
  data,
});

export const APPLY_JOB_FILTERS = (data) => ({
  type: 'APPLY_JOB_FILTERS',
  data,
});

export const FETCH_PROJECT_DETAILS = (data) => ({
  type: 'FETCH_PROJECT_DETAILS',
  data,
});

export const CREATE_PROPOSAL = (data) => ({
  type: 'CREATE_PROPOSAL',
  data,
});

export const CURRENT_FILTERS = (data) => ({
  type: 'CURRENT_FILTERS',
  data,
});

export const CLEAR_FILTERS = (data) => ({
  type: 'CLEAR_FILTERS',
  data,
});
